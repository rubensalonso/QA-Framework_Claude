# Configuración por defecto de la prueba de carga (valores deliberadamente bajos: sitio público).
# Cualquier opción se puede sobreescribir por CLI, p. ej.: locust --config ... --run-time 2m
locustfile = performance/load/locustfile.py
host = https://automationexercise.com
headless = true
users = 3
spawn-rate = 1
run-time = 1m
only-summary = true
html = reports/load-test-report.html
csv = reports/load-test
